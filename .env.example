# Orris Environment Variables Example
# Copy this file to .env and modify values as needed
#
# IMPORTANT: Environment variables use the ORRIS_ prefix and underscore separators
# Format: ORRIS_<SECTION>_<KEY>
# Example: ORRIS_SERVER_PORT=8080
#
# Priority: Environment variables > config.yaml > defaults
#
# You can choose to configure using:
# 1. Environment variables only (recommended for production)
# 2. config.yaml only (good for development)
# 3. Mix of both (env vars override config.yaml)

# ============================================================
# Server Configuration
# ============================================================
ORRIS_SERVER_HOST=0.0.0.0
ORRIS_SERVER_PORT=8081
ORRIS_SERVER_MODE=debug  # Options: debug, release, test

# ============================================================
# Database Configuration
# ============================================================
ORRIS_DATABASE_HOST=localhost
ORRIS_DATABASE_PORT=3306
ORRIS_DATABASE_USERNAME=orris

# CRITICAL: Set a secure database password
# For production, NEVER commit the actual password to version control
ORRIS_DATABASE_PASSWORD=your_secure_password_here

ORRIS_DATABASE_DATABASE=orris_dev
ORRIS_DATABASE_MAX_IDLE_CONNS=10
ORRIS_DATABASE_MAX_OPEN_CONNS=100
ORRIS_DATABASE_CONN_MAX_LIFETIME=60  # minutes

# ============================================================
# Logger Configuration
# ============================================================
ORRIS_LOGGER_LEVEL=info  # Options: debug, info, warn, error
ORRIS_LOGGER_FORMAT=console  # Options: json, console
ORRIS_LOGGER_OUTPUT_PATH=stdout  # Options: stdout, stderr, or file path (e.g., /var/log/orris.log)

# ============================================================
# Usage Examples
# ============================================================
# Development (using config.yaml):
#   - Don't set any environment variables
#   - Edit configs/config.yaml instead
#
# Production (using environment variables):
#   - Set ORRIS_DATABASE_PASSWORD as environment variable
#   - Optionally override other settings
#
# Docker (environment variables via docker-compose.yml):
#   environment:
#     - ORRIS_DATABASE_HOST=mysql
#     - ORRIS_DATABASE_PASSWORD=${DB_PASSWORD}
#
# Kubernetes (using ConfigMap and Secrets):
#   - ConfigMap for non-sensitive configs
#   - Secret for ORRIS_DATABASE_PASSWORD

# ============================================================
# Authentication Configuration
# ============================================================
# Password hashing cost (10-14, higher is more secure but slower)
ORRIS_AUTH_PASSWORD_BCRYPT_COST=12

# Token expiration settings
ORRIS_AUTH_TOKEN_VERIFICATION_EXPIRES_HOURS=24
ORRIS_AUTH_TOKEN_RESET_EXPIRES_MINUTES=30

# JWT Configuration
# CRITICAL: Change this secret in production! Use a long random string
ORRIS_AUTH_JWT_SECRET=your-super-secret-jwt-key-change-in-production
ORRIS_AUTH_JWT_ACCESS_EXP_MINUTES=15
ORRIS_AUTH_JWT_REFRESH_EXP_DAYS=7

# Session Configuration
# Default session duration (when "remember me" is NOT checked)
ORRIS_AUTH_SESSION_DEFAULT_EXP_DAYS=1
# Remember me session duration (when "remember me" IS checked)
ORRIS_AUTH_SESSION_REMEMBER_EXP_DAYS=30

# ============================================================
# OAuth Configuration (Google)
# ============================================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
ORRIS_OAUTH_GOOGLE_CLIENT_ID=your-google-client-id
ORRIS_OAUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret
ORRIS_OAUTH_GOOGLE_REDIRECT_URL=http://localhost:8081/auth/oauth/google/callback

# ============================================================
# OAuth Configuration (GitHub)
# ============================================================
# Get credentials from: https://github.com/settings/developers
ORRIS_OAUTH_GITHUB_CLIENT_ID=your-github-client-id
ORRIS_OAUTH_GITHUB_CLIENT_SECRET=your-github-client-secret
ORRIS_OAUTH_GITHUB_REDIRECT_URL=http://localhost:8081/auth/oauth/github/callback

# ============================================================
# Email Configuration (SMTP)
# ============================================================
# For development, you can use MailHog (localhost:1025)
# For production, use a real SMTP service (e.g., SendGrid, AWS SES, Gmail)
ORRIS_EMAIL_SMTP_HOST=localhost
ORRIS_EMAIL_SMTP_PORT=1025
ORRIS_EMAIL_SMTP_USER=
ORRIS_EMAIL_SMTP_PASSWORD=
ORRIS_EMAIL_FROM_ADDRESS=noreply@orris.local
ORRIS_EMAIL_FROM_NAME=Orris