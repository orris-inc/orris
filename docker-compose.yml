version: '3.8'

services:
  caddy:
    image: caddy:2-alpine
    container_name: orris_caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - orris

  frontend:
    image: ghcr.io/orris-inc/orris-frontend:latest
    container_name: orris_frontend
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      - orris

  orris:
    image: ghcr.io/orris-inc/orris:latest
    container_name: orris_app
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - ./configs:/app/configs:ro
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  mysql:
    image: mysql:8.0
    container_name: orris_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-password}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-orris}
      MYSQL_USER: ${MYSQL_USER:-orris}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-orris_password}
    ports:
      - "3306:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: orris_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: redis-server --appendonly yes

volumes:
  caddy_data:
  caddy_config:
